# Task: count_unique speedup

`solution.py` contains a correct but slow `count_unique(values)` (O(n^2)).

Rewrite it so it is:
1. still correct (returns the number of distinct values), and
2. at least 10x faster than the O(n^2) baseline on a 10000-element benchmark.

Keep the function name `count_unique` and the single-argument signature.
Do not use any third-party library; the standard library is allowed.
