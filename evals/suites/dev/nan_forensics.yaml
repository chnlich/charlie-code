id: nan_forensics
prompt: |
  A training run is crashing instead of producing a finite loss. Running
  `python train.py` raises a numerical overflow exception from cross_entropy
  in loss.py on the logits [400.0, 1000.0, 600.0] with target 0. Investigate
  loss.py, find the root cause of the numerical instability, and fix it so
  that `python train.py` runs to completion and prints a finite final_loss, and
  cross_entropy([400.0, 1000.0, 600.0], 0) returns a finite number. Fix the
  numerical guard in the loss computation; do not work around it by removing
  data or changing the inputs in train.py. See TASK.md in the working directory
  for details.
fixture: fixtures/nan_forensics
grade: grade/nan_forensics.py
timeout_s: 60
step_limit: 20
tags: [pilot, fault-localization, numerics]
